package ru.spbgrup.test;

public class Farmer {
    int resource = 5;
    public void takeResource(Pet[] WildAnimal){
        for (int i = 0; i < WildAnimal.length ; i++) {
            if (WildAnimal[i] instanceof )
            resource += WildAnimal[i].resource;
        }
    }

}
